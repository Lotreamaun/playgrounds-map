## 1. Геолокация в фоне при входе в режим добавления

- [x] 1.1 В `App.tsx:handleAddToggle` при включении `addMode` вызывать `navigator.geolocation.getCurrentPosition` с `enableHighAccuracy: true`, `timeout: 8000`, `maximumAge: 30000`
- [x] 1.2 Ввести `locating: boolean` и `locationFailed: boolean`, отражающие состояние фонового запроса
- [x] 1.3 На обеих платформах форма (`sheet: { type: 'form' }`) открывается сразу при входе в `addMode`, не дожидаясь ответа геолокации
- [x] 1.4 При успехе — установить `pick` из координат ответа в фоне (форма уже открыта, координаты и адрес подтягиваются в неё по мере ответа)
- [x] 1.5 При ошибке/недоступности `navigator.geolocation` — `pick` не задаётся, форма остаётся открытой с `locationFailed: true`
- [x] 1.6 Защититься от гонки: если пользователь отменил добавление (`handleCancelAdd`) или выключил `addMode` до ответа геолокации, поздний колбэк не должен переоткрывать форму/перезаписывать состояние

## 2. Обратная связь в карточке

- [x] 2.1 Пока запрос выполняется — в поле координат формы показывать «Определяем местоположение…» (не на кнопке/FAB)
- [x] 2.2 Кнопка «Указать на карте» на мобильном: сворачивает полноэкранную форму, оставляя режим выбора точки (cancel-FAB + подсказка); тап по карте ставит точку и возвращает форму с координатами
- [x] 2.3 Desktop: при неудаче показывать подсказку в карточке (карта видна рядом — клик по ней сразу ставит точку, кнопка не требуется)
- [x] 2.4 Убедиться, что `locating` и `locationFailed` сбрасываются во всех исходах (успех, ошибка, таймаут, ручной клик, отмена)

## 3. Видимый маркер выбранной точки

- [x] 3.1 Прокинуть `pick: Coordinates | null` пропом из `App.tsx` в `MapView`
- [x] 3.2 В `MapView` рендерить `YMapMarker` для `pick` вне `YMapClusterer`/слоя `courts`, визуально отличимый от `CourtMarker` (черновик точки, без иконки вида спорта)
- [x] 3.3 При изменении `pick` (повторный клик по карте) маркер должен переходить на новые координаты, а не дублироваться
- [x] 3.4 При отмене/закрытии формы (`pick === null`) маркер должен исчезать

## 4. Ручная коррекция и адрес

- [x] 4.1 Убедиться, что клик по карте в режиме добавления продолжает работать как раньше и переопределяет как auto-, так и ранее кликнутую точку (существующая логика `onMapClick`/`handleMapClick`, проверена — `handleMapClick` сохраняет прежнее переопределение `pick` и открытие формы)
- [x] 4.2 Убедиться, что `CourtForm` по-прежнему ре-геокодирует адрес при каждом новом значении `coordinates`, включая координаты, пришедшие из геолокации (существующий `useEffect` в `CourtForm.tsx`, ключ `[coordinates]` — проверено без изменений)

## 5. Центрирование карты

- [x] 5.1 При успешном определении точки по геолокации центрировать карту на `pick` (использовать существующий `mapInstanceRef.setLocation`, аналогично центрированию на `selectedCourt`)

## 6. Ручная проверка

- [ ] 6.1 Desktop: разрешить геолокацию → форма открывается сразу, координаты/адрес подставляются фоном, карта центрируется и появляется маркер
- [x] 6.2 Desktop: запретить геолокацию → форма открывается с сообщением «Не удалось определить местоположение», клик по карте ставит точку (карта видна рядом)
- [ ] 6.3 Мобильный: разрешить геолокацию → полноэкранная форма сразу, статус «Определяем местоположение…», затем координаты/адрес заполняются автоматически
- [x] 6.4 Мобильный: запретить/эмулировать таймаут → в форме сообщение + кнопка «Указать на карте»; по нажатию форма сворачивается, тап по карте ставит точку и возвращает форму с координатами
- [ ] 6.5 После авто-точки кликнуть по другой точке карты → координаты, адрес и маркер обновляются на новую точку
- [x] 6.6 Отменить добавление во время фоновой геолокации → поздний ответ геолокации не открывает форму повторно