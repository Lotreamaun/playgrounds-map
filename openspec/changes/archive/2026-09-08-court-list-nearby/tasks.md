## 1. API client: fetch all courts

- [x] 1.1 В `frontend/src/services/api.ts` сделать параметр bbox в `getCourts` необязательным (`bbox?: BBox`); при его отсутствии не добавлять query-параметры
- [x] 1.2 Убедиться, что при вызове без bbox запрос `GET /courts` возвращает все площадки (проверить ручным запросом/swagger), и что существующие вызовы с bbox не сломались

## 2. Distance utility

- [x] 2.1 Добавить утилиту вычисления расстояния по формуле Хаверсина (`distanceHaversine(lat1, lon1, lat2, lon2)`) в отдельный модуль `frontend/src/utils/` (или аналогично принятой структуре проекта)
- [x] 2.2 Написать метод сортировки массива площадок по расстоянию от заданной точки (лат/лон) возрастанием

## 3. CourtList component

- [x] 3.1 Создать компонент `CourtList` (`frontend/src/components/CourtList.tsx`), принимающий список площадок, функцию выбора площадки и признак доступности геолокации
- [x] 3.2 Отображать каждую площадку строкой-карточкой: адрес (`address`, с заглушкой при его отсутствии), покрытие, состояние
- [x] 3.3 Обработать выбор строки (клик) — вызвать колбэк выбора с площадкой
- [x] 3.4 Реализовать переключатель сортировки «по близости» (по расстоянию от геолокации), с понятным поведением при недоступной геолокации (фолбэк без ошибки)

## 4. App state and view toggle

- [x] 4.1 В `App.tsx` добавить состояние `view: 'map' | 'list'` и `selectedCourt: Court | null`
- [x] 4.2 Добавить переключатель «карта / список», рендерящий либо `MapView`, либо `CourtList`
- [x] 4.3 При необходимости догрузить полный набор площадок при входе в список, если он ещё не загружен полностью
- [x] 4.4 Обработчик клика по строке списка: установить `selectedCourt` и (по желанию) переключить вид на карту

## 5. Map ↔ list integration

- [x] 5.1 Передать `selectedCourt` в `MapView` и по его изменению/монтированию центрировать карту на его координатах и открывать его карточку (переиспользуя popup из `CourtMarker.tsx`)
- [x] 5.2 Убедиться, что переключение вида в обе стороны работает корректно и карта центрируется на выбранной площадке

## 6. Verification

- [x] 6.1 Прогнать `npm run lint` и `npm run build` во `frontend`
- [x] 6.2 Запустить frontend и backend, проверить полный сценарий: переключение карта↔список, отображение адресов, сортировку «по близости» (с геолокацией и без), клик по строке → центрирование карты с карточкой
