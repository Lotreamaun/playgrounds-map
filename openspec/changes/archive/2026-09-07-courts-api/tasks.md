## 1. Сервис-слой (`services/court_service.py`)

- [x] 1.1 Валидация координат и обязательных полей площадки (422 при нарушении)
- [x] 1.2 Дедупликация: поиск площадок того же вида спорта в радиусе ~30 м (Haversine) перед созданием (409 при дубле)
- [x] 1.3 Валидация фото (jpg/png, макс. 5 МБ) и сохранение на диск (400 при нарушении), `photo_url` указывает на сохранённый файл

## 2. Роутер (`routers/courts.py`)

- [x] 2.1 `GET /courts` — фильтр по bounding box
- [x] 2.2 `POST /courts` (`multipart/form-data`, с фото) — 201 с телом созданной записи
- [x] 2.3 `GET /courts/{id}` — 200 для существующего id, 404 для несуществующего
- [x] 2.4 `DELETE /courts/{id}` — защищено `ADMIN_TOKEN` (401/403 без токена, 204 с верным токеном)

## 3. Интеграция с приложением

- [x] 3.1 Регистрация роутера `courts` в `main.py` (из `backend-foundation`)

## 4. Проверка

- [x] 4.1 Ручная проверка каждого эндпоинта через Swagger UI (`/docs`) без ошибок 500
- [x] 4.2 Повторная отправка той же площадки (тот же спорт, координаты в радиусе 30 м) → 409, дубликат не создан
- [x] 4.3 Площадка без фото и с фото — оба сценария создания проходят без ошибок
