## 1. Геолокация при входе в режим добавления

- [ ] 1.1 В `App.tsx:handleAddToggle` при включении `addMode` вызывать `navigator.geolocation.getCurrentPosition` с `enableHighAccuracy: true`, `timeout: 8000`, `maximumAge: 30000`
- [ ] 1.2 Ввести состояние ожидания геолокации (например, `locating: boolean`), устанавливаемое на время запроса
- [ ] 1.3 При успехе — установить `pick` из координат ответа и открыть форму (`sheet: { type: 'form' }`) сразу, на обеих платформах
- [ ] 1.4 При ошибке/недоступности `navigator.geolocation` — оставить текущее поведение: `pick` не задаётся, desktop открывает форму с пустыми координатами, мобильный остаётся в состоянии «тап по карте» (cancel-FAB + подсказка)
- [ ] 1.5 Защититься от гонки: если пользователь отменил добавление (`handleCancelAdd`) или выключил `addMode` до ответа геолокации, поздний колбэк не должен переоткрывать форму/перезаписывать состояние

## 2. Индикатор ожидания

- [ ] 2.1 Показать состояние ожидания на desktop-кнопке «Добавить площадку», пока `locating === true` (например, текст «Определяем местоположение…»)
- [ ] 2.2 Показать то же состояние на мобильном FAB «Добавить»
- [ ] 2.3 Убедиться, что состояние снимается во всех исходах (успех, ошибка, таймаут)

## 3. Видимый маркер выбранной точки

- [ ] 3.1 Прокинуть `pick: Coordinates | null` пропом из `App.tsx` в `MapView`
- [ ] 3.2 В `MapView` рендерить `YMapMarker` для `pick` вне `YMapClusterer`/слоя `courts`, визуально отличимый от `CourtMarker` (черновик точки, без иконки вида спорта)
- [ ] 3.3 При изменении `pick` (повторный клик по карте) маркер должен переходить на новые координаты, а не дублироваться
- [ ] 3.4 При отмене/закрытии формы (`pick === null`) маркер должен исчезать

## 4. Ручная коррекция и адрес

- [ ] 4.1 Убедиться, что клик по карте в режиме добавления продолжает работать как раньше и переопределяет как auto-, так и ранее кликнутую точку (существующая логика `onMapClick`/`handleMapClick`, изменений не требует, но нужно покрыть проверкой после рефакторинга п.1)
- [ ] 4.2 Убедиться, что `CourtForm` по-прежнему ре-геокодирует адрес при каждом новом значении `coordinates`, включая координаты, пришедшие из геолокации (существующий `useEffect` в `CourtForm.tsx`, проверить работу без изменений)

## 5. Центрирование карты

- [ ] 5.1 При успешном определении точки по геолокации центрировать карту на `pick` (использовать существующий `mapInstanceRef.setLocation`, аналогично центрированию на `selectedCourt`)

## 6. Ручная проверка

- [ ] 6.1 Desktop: разрешить геолокацию → «Добавить площадку» сразу открывает форму с координатами и маркером на карте
- [ ] 6.2 Desktop: запретить геолокацию → форма открывается пустой, с подсказкой кликнуть по карте, как сегодня
- [ ] 6.3 Мобильный: разрешить геолокацию → FAB показывает ожидание, затем сразу открывается полноэкранная форма
- [ ] 6.4 Мобильный: запретить/эмулировать таймаут геолокации → откат на текущий сценарий (cancel-FAB + подсказка «Нажмите на карту»)
- [ ] 6.5 После авто-точки кликнуть по другой точке карты → координаты, адрес и маркер обновляются на новую точку
- [ ] 6.6 Отменить добавление во время ожидания геолокации → поздний ответ геолокации не открывает форму повторно
